import { MapeadorTable } from "@/components/Mapeador/MapeadorTable";

const MapeadorPage = () => {
  return (
    <div className="container mx-auto py-10">
      <MapeadorTable />
    </div>
  );
};

export default MapeadorPage;
